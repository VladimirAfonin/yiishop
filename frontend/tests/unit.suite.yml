class_name: UnitTester
modules:
    enabled:
        - Yii2:
            part: [orm, email, fixtures]
        - Asserts
        - Db
    config:
        Db:
          dsn: 'sqlite:/../data/test.db'
          user: ''
          password: ''
          dump: tests/_data/dump.sql